version: 1.0
runtime: python311

build:
  commands:
    build:
      - python3 -m pip install --upgrade pip
      - python3 -m pip install -r requirements.txt

run:
  command: export PYTHONPATH=$PYTHONPATH:. && python3 -m uvicorn src.main:app --host 0.0.0.0 --port 8080
  network:
    port: 8080
